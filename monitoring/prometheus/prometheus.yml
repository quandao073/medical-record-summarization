global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "medical-summarization-api"
    metrics_path: "/api/v1/metrics"
    static_configs:
      - targets: ["api:8000"]
        labels:
          service: "summarization-api"
